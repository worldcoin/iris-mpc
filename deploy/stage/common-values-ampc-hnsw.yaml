image: "ghcr.io/worldcoin/iris-mpc-cpu:6e709de788d8461b1ce4e8a89c8120a993f7dbfe"

environment: stage
replicaCount: 1

strategy:
  type: Recreate

datadog:
  enabled: true

ports:
  - containerPort: 3000
    name: health
    protocol: TCP

livenessProbe:
  httpGet:
    path: /health
    port: health

readinessProbe:
  periodSeconds: 30
  httpGet:
    path: /ready
    port: health

startupProbe:
  initialDelaySeconds: 60
  failureThreshold: 120
  periodSeconds: 30
  httpGet:
    path: /ready
    port: health

resources:
  limits:
    cpu: 21
    memory: 30Gi
  requests:
    cpu: 20
    memory: 30Gi

imagePullSecrets:
  - name: github-secret

nodeSelector:
  kubernetes.io/arch: amd64

podSecurityContext:
  runAsUser: 65534
  runAsGroup: 65534
