# Per-node overrides; shared vars live in common-values-ampc-hnsw-anon-stats-server.yaml.
env:
  SMPC__SERVICE__SERVICE_NAME: "anon-stats-server-hnsw-1"
  SMPC__SERVER_COORDINATION__PARTY_ID: "1"
  SMPC__SERVER_COORDINATION__NODE_HOSTNAMES: '["anon-stats-server.0.stage.hnsw.worldcoin.dev","0.0.0.0","anon-stats-server.2.stage.hnsw.worldcoin.dev"]'
  SMPC__SERVICE__METRICS__PREFIX: "anon-stats-server-hnsw-1"

initContainer:
  enabled: true
  image: "amazon/aws-cli:2.17.62"
  name: "anon-stats-server-hnsw-dns-records-updater"
  env:
    - name: PARTY_ID
      value: "1"
    - name: POD_IP
      valueFrom:
        fieldRef:
          fieldPath: status.podIP
  configMap:
    init.sh: |
      #!/usr/bin/env bash

      # Set up environment variables
      HOSTED_ZONE_ID=$(aws route53 list-hosted-zones-by-name --dns-name "$PARTY_ID".stage.hnsw.worldcoin.dev --query "HostedZones[0].Id" --output text)

      # Generate the JSON content in memory
      BATCH_JSON=$(cat <<EOF
      {
        "Comment": "Upsert the A record for anon-stats-server-hnsw pod",
        "Changes": [
          {
            "Action": "UPSERT",
            "ResourceRecordSet": {
              "Name": "anon-stats-server.$PARTY_ID.stage.hnsw.worldcoin.dev",
              "TTL": 5,
              "Type": "A",
              "ResourceRecords": [{
                "Value": "$POD_IP"
              }]
            }
          }
        ]
      }
      EOF
      )

      # Execute AWS CLI command with the generated JSON
      aws route53 change-resource-record-sets --hosted-zone-id "$HOSTED_ZONE_ID" --change-batch "$BATCH_JSON"


attachSecurityGroupPolicy:
  enabled: true
  groupIds:
    - sg-0c6d58cc85ebadac3
